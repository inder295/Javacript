//memory allocation in variable.

let name="Inder";
let name2=name;

console.log(name2);


name='karan';
console.log(name);

//name2 value not get changed because name2 copies the value from name1 not the refernce address. 